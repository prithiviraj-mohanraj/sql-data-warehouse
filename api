import pandas as pd

cloudera_counts = pd.read_csv("cloudera_oldest_counts.csv")

# normalize case
cloudera_counts['schema'] = cloudera_counts['schema'].str.lower()
cloudera_counts['table'] = cloudera_counts['table'].str.lower()

valid_rows = cloudera_counts.dropna(subset=['oldest_business_day'])
valid_rows = valid_rows[valid_rows['oldest_business_day'] != ""]

tables_with_bd = sf_columns[
    sf_columns['column_name'] == 'business_day'
][['schema','table']].drop_duplicates()

valid_rows = valid_rows.merge(
    tables_with_bd,
    on=['schema','table'],
    how='inner'
)

snowflake_results = []

for _, row in valid_rows.iterrows():
    schema = row['schema']
    table = row['table']
    oldest = row['oldest_business_day']

    try:
        query = f"""
        SELECT COUNT(*) AS record_count
        FROM prod.{schema}.{table}
        WHERE business_day = '{oldest}'
        """

        df = session.sql(query).to_pandas()

        count_val = df.iloc[0,0] if not df.empty else 0

        snowflake_results.append({
            "schema": schema,
            "table": table,
            "oldest_business_day": oldest,
            "record_count": count_val
        })

    except Exception as e:
        print(f"Skipped {schema}.{table}: {e}")

snowflake_counts_df = pd.DataFrame(snowflake_results)
snowflake_counts_df

comparison = cloudera_counts.merge(
    snowflake_counts_df,
    on=['schema','table','oldest_business_day'],
    how='left',
    suffixes=('_cloudera','_snowflake')
)

comparison['match'] = (
    comparison['record_count_cloudera'] ==
    comparison['record_count_snowflake']
)

comparison

mismatch_counts = comparison[comparison['match'] == False]
mismatch_counts
